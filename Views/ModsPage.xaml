<Page x:Class="StoryForge.Views.ModsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="{StaticResource BgBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> 
            <RowDefinition Height="Auto"/>  
            <RowDefinition Height="*"/>    
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="{StaticResource SurfaceBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="0,0,0,1"
                Padding="28,16,28,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Mod Manager"
                               Style="{StaticResource SectionTitle}"/>
                    <TextBlock Text="Install, enable, disable, and remove mods for MCSM."
                               Style="{StaticResource McLabel}"
                               Margin="0,2,0,0"/>
                </StackPanel>
                <Button Grid.Column="1"
                        Content="+ Install Mod"
                        Style="{StaticResource GoldButton}"
                        Padding="16,9"
                        VerticalAlignment="Center"
                        Click="Install_Click"/>
            </Grid>
        </Border>

        <Border Grid.Row="1"
                Background="{StaticResource BgBrush}"
                Padding="28,10,28,10">
            <TextBlock x:Name="ModCountLabel"
                       Text="0 mods installed"
                       Style="{StaticResource McLabel}"/>
        </Border>

        <ScrollViewer Grid.Row="2"
                      VerticalScrollBarVisibility="Auto"
                      Padding="28,8,28,28">
            <StackPanel x:Name="ModList"/>
        </ScrollViewer>
    </Grid>
</Page>
